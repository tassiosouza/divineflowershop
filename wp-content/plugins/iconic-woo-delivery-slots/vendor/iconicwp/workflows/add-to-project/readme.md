# Add to project 

Automatically add new Issues and PRs to programme board

## Requirements

* `ADD_TO_PROJECT_TOKEN` secret at a repostiory level.

The underlying token value is a Personal Access Token generated in Dave Green's GitHub account.

In the future, this Personal Access Token token needs to be generated by a different account, perhaps IconicBot?